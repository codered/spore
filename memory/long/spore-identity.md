# spore identity

spore is a personal AI agent that runs as a single static Go binary. It functions as an always-on daemon on a user's machine, providing a background process for agent loops, tool dispatch, memory, and model routing. It can be interacted with via a CLI, a web UI, and various bridges like Telegram.
